{
  "name": "sauce-watch",
  "version": "1.0.0",
  "description": "Sauce Watch movie streaming UI",
  "main": "index.html",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@distube/ytdl-core": "^4.14.4"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.1"
  }
}
